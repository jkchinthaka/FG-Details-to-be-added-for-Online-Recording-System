# OBSOLETE — PostgreSQL backup/restore scripts

These scripts (`backup.ps1`, `backup.sh`, `restore.ps1`, `reconcile_counts.sql`) target **PostgreSQL**.

**Do not use for MongoDB Atlas recovery.**

Canonical tooling: `scripts/disaster-recovery/`  
Canonical docs: `docs/database/MONGODB_BACKUP_RESTORE_RUNBOOK.md`

Retained only so historical references do not break links.
