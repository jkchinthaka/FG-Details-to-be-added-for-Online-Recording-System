NEXT_PUBLIC_API_URL=http://localhost:3001

# Production Worker (set in Cloudflare dashboard / wrangler vars — never commit secrets):
# NEXT_PUBLIC_API_URL=https://fg-api.nelna.lk
#
# Optional server-side override for middleware -> API (same value in Workers):
# API_INTERNAL_URL=https://fg-api.nelna.lk
#
# Never set DATABASE_URL on the Cloudflare Worker.
